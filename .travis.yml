language: ruby

cache: bundler

sudo: false

before_install:
  - gem update bundler

rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.10
  - 2.2.8
  - 2.3.5
  - 2.4.2
  - ruby-head
  - jruby

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby
  fast_finish: true
