language: ruby

cache: bundler

sudo: false

before_install:
  - gem update bundler

rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.10
  - 2.2.7
  - 2.3.4
  - 2.4.1
  - ruby-head
  - jruby

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby
  fast_finish: true
